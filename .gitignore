# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node (future Next.js frontend)
node_modules/
.next/
dist/
*.log

# .NET
bin/
obj/
*.user
.vs/
TestResults/

# Secrets / local config
.env
.env.*
!.env.example
*.local
appsettings.*.local.json

# Confidential calibration footage — the internal demo + its derived seg2 window.
# Fetched from the EU dev bucket by scripts/fetch-calibration.sh; NEVER committed
# (company-confidential, ~293 MB). The tests skip when it is absent.
.calibration/

# Key material — must never exist in the repo (DEVELOPMENT.md §6: ADC only, no key files)
*-key.json
*service-account*.json
*.pfx
*.pem
*.p12

# Terraform (future infra)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
