// The Phase 2R wordmark: monospace "mdreel" with the blue caret added by the .brand CSS pseudo-element.
// `md` is brand-blue; the rest is ink. Kept inline so it inherits header/footer sizing via .brand.
export function BrandMark() {
  return (
    <>
      <b>md</b>reel
    </>
  );
}
