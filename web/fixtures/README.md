# Fixtures

Generated by `scripts/sync-fixtures.mjs` from `experiments/001-gemini-video-benchmark/out/`.
**Do not hand-edit.** Re-run `npm run sync-fixtures` after the experiment output changes, then
commit the result — `experiments/` is invisible to the Cloud Run source-deploy build context, so
the app reads these mirrored copies at request time, not the originals.
